{
  "name": "fin-flow-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:ui": "vite build && node scripts/embed-ui.mjs",
    "build": "npm run build:ui",
    "dev": "npm run build:ui && wrangler dev",
    "deploy": "npm run build:ui && wrangler deploy"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.2",
    "vite": "^8.0.16",
    "wrangler": "^4.16.1"
  },
  "dependencies": {
    "hono": "^4.12.25",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  }
}
